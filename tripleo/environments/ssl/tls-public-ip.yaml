# This environment file is used when enabling tls-endpoints-public-ip

parameter_defaults:
  EndpointMapOverride:
    ExampleServicePublic:
      protocol: 'https'
      port: '13181'
      host: 'IP_ADDRESS'
